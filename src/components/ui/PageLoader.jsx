const PageLoader = () => {
  return (
    <div style={{
      height: "70vh",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      fontSize: "1.2rem"
    }}>
      Loading...
    </div>
  );
};

export default PageLoader;
